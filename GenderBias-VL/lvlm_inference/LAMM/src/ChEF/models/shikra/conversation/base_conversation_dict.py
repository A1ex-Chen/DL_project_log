def dict(self):
    return {'name': self.name, 'system': self.system, 'roles': self.roles,
        'messages': self.messages, 'offset': self.offset, 'conv_id': self.
        conv_id, 'model_name': self.model_name}
