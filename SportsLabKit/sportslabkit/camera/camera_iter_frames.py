def iter_frames(self, calibrate: bool=False, crop: bool=False) ->Generator[
    NDArray, None, None]:
    """Iterate over frames of video.

        Yields:
            NDArray: frame of video.
        """
    return self
