def __init__(self, N, connected_samples=False, random=True,
    spatial_completion=False):
    self.N = N
    self.connected_samples = connected_samples
    self.random = random
    self.spatial_completion = spatial_completion
